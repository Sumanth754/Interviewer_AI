# Copy this file to .env.local for local overrides. All values optional.

# Dev (default): leave blank — Vite's proxy sends /api to http://localhost:5000.
# Production build of the React app, deployed separately from the API:
VITE_API_BASE=https://your-api-host.example.com